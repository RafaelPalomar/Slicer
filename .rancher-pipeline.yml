stages:
- name: Build Environment
  steps:
  - publishImageConfig:
      dockerfilePath: Docker/Dockerfile.ubuntu.hirstue
      buildContext: .
      tag: slicer-ubuntu-hirstue
timeout: 60
notification: {}
